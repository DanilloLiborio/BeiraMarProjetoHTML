// Exemplo simples de script para interação futura
// Pode ser expandido com filtros, modais, API, etc.

document.addEventListener("DOMContentLoaded", () => {
  const filterBtn = document.querySelector(".filter-btn");

  filterBtn.addEventListener("click", () => {
    alert("Função de filtros ainda será implementada.");
  });
});